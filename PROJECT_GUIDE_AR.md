# شرح المشروع (مختصر)

## الفكرة
هذا المشروع مخصص لاكتشاف الأسلحة في الفيديوهات والبث المباشر، ثم عرض النتائج في لوحة ويب.

## المكونات
- `app.py`: خادم Flask الرئيسي.
- `camera/`: واجهات الكاميرات، رفع الفيديو، بدء الفحص، وحالة الفحص.
- `event/`: إدارة أحداث الإنذار الناتجة عن الاكتشاف.
- `wepapp/`: منطق ونماذج اكتشاف السلاح (TensorFlow/YOLO).
- `frontend/`: واجهة Next.js لرفع الفيديو، تشغيل الفحص، ومشاهدة النتائج.
- `webhook_sample/`: خدمة Socket/Webhook للإشعارات الفورية.

## المنافذ
- الواجهة: `3000`
- الـ API: `5000`
- الـ Webhook/Socket: `5001`

## التشغيل السريع
من داخل جذر المشروع:

```powershell
.\run_project.ps1
```

بعد التشغيل:
- افتح الواجهة: `http://127.0.0.1:3000`
- API: `http://127.0.0.1:5000/api`

## طريقة معالجة الفيديو
1. رفع الفيديو من الواجهة.
2. بدء فحص الفيديو.
3. الواجهة تستدعي `scan-video/status` بشكل دوري.
4. يظهر شريط التقدم + الوقت المتبقي (ETA) + النتائج.

## ملاحظة عن اللقطات
- كلما قل `snapshot interval` زادت فرص التقاط لقطات أكثر.
- `1s` يعطي كثافة أعلى للقطات مقارنة بالقيم الأكبر.
